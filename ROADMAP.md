# NexusSky（天枢）演进路线图

> 面向「单终端 / 多终端」两类场景，逐步补齐 6 类需求 × 10 项能力 × 3 类硬件交互。
> 核心哲学延续项目基线：**软件协议层先行、数据源可替换**——真硬件接入时替换的是
> 数据源而非架构。每个里程碑走完整 SDD（Spec → Design → Tasks → Develop → Test → Review → Verify）。

## 一、现状基线（已具备，无需重做）

- **协议栈**：MAVLink v1/v2 编解码 + UDP 传输，CRC 与官方逐字节一致（30+ 消息）
- **单机闭环**：任务上传 → ARM → 航点飞行 → 拍照/目标跟踪/地理定位 → RTL → failsafe → 日志
- **多机路由**：sysid→源地址路由表，命令不串线（P0）
- **物理引擎 v2**：加速度/协调转弯/bank/姿态，传感器故障注入（P1）
- **链路损伤**：延迟/丢包(Gilbert-Elliot)/带宽/分区 + 7 画像（link-sim）
- **机载 failsafe**：链路/电量/GPS 三触发，PX4 语义
- **感知闭环**：合成目标世界 → 相机投影 → 地理定位 → 目标跟踪（投影简化）
- **角色调度**：Leader/Worker/Relay 状态机 + 多目标分配（Batch F，可解释规则）
- **灾害应急通讯组网**：QoS 优先级队列 + 分簇路由 + 异构链路桥接 + Budget 模式 +
  丐版 Mesh(ESP-NOW/LoRa) + AMG8833 热源搜救 + 卫星中继增强(天通/铱星/星链) +
  厂商协议适配(海康/大华/宇视/ONVIF) + 灾区通信隔离 + 灾害态势面板（P2）
- **动态 MAX_HOPS 集成（FR-18）**：DynamicMaxHops 按网络节点数自适应跳数上限
  （≤20→15, 21-50→20, >50→25, 上限30），`dynamicMaxHopsEnabled` 默认 false 向后兼容（P3）
- **真实卫星接入预留**：天通/铱星/星链三种卫星通信系统占位实现类，统一 SatelliteLink
  接口框架，为真实卫星硬件接入预留接口（P3）
- **代码审查**：5 轮收敛性审查完成，累计修复 21 个问题（4C + 12M + 5m），3230 单测全绿
- **新功能扩展方案全部完成**：P0–P3 + Wave 1-2（M0a–M13、4a、P2、P3）+ 代码审查全部收尾

## 二、分阶段里程碑

### M0 基座补齐（通信 + 环境，两条线可并行）
| 编号 | 方向 | 交付物 | 依赖 |
|---|---|---|---|
| M0a | **mesh 组网落地** | 一跳静态中继代码 + 单测 + e2e-mesh，预留动态路由扩展点 | mesh-relay-design.md（方案已细化） |
| M0b | **环境气象机制** | 温度/湿度/天气/风力模型 + 告警 + 对应 MAVLink 消息 | 无 |

- M0a 是「多终端」的通信地基；M0b 是「喷洒(风)/物流(天气)/表演(风)」的横切能力。
- **第一批先做 M0a**：方案已细化、风险最低、见效最快，且是 M1 的前置。

### M1 多终端协同演示切片（编队表演 + 灯光）——用户首选
| 交付物 | 说明 |
|---|---|
| 编队队形生成 | 几何队形（横列/纵列/V 形/圆环）导航点下发 |
| 动作同步 | 队内时间/相位同步（基于心跳时钟），多机齐动 |
| 灯光控制 | MAVLink LED/灯光命令建模 + 模拟器灯效 + GCS 观察 |
| e2e | 3+ 机编队起飞 → 队形变换 → 灯光闪烁 → 返航，全程可演示 |

依赖：M0a（多机通信）、Baseline（角色调度 Batch F）。

### M2 行业应用：执行机构抽象 + 喷洒/物流
| 交付物 | 说明 |
|---|---|
| Actuator 抽象 | 通用执行机构接口（喷洒泵/抛投器），模拟器造假数据源 |
| 喷洒任务类型 | 航线喷洒 + 流量控制 + 药量监测（受 M0b 风影响） |
| 抛投/物流 | 抓取/投放机构 + 负载模型 + 配送站点序列 |

依赖：M0b（风/天气）、M0a（集群作业）。

### M3 感知成像增强：多光谱/热成像/避障
| 交付物 | 说明 |
|---|---|
| 真实 CV 接入点 | 把投影简化替换为模型输出 (u,v,kind) 的接入点做实 |
| 多光谱/热成像 | 遥感载荷抽象（NDVI/温度场），替换数据源不变链路 |
| 避障/深度感知 | 空间感知升级：深度/点云抽象 + 避障命令 |

依赖：Baseline（CaptureService 真值通道已预留替换点）。

### M4 硬件数据接入抽象：相控阵雷达/动力气动/其余遥感
| 交付物 | 说明 |
|---|---|
| 相控阵雷达载荷 | 协议抽象 + 扫描/目标接入（软件层，接真雷达时替换数据） |
| 动力系统气动级 | 旋翼气动模型升级（推力/转速/桨效，替换 DronePhysics 运动学） |
| 遥感扩展 | LiDAR/IMU 融合等（M3 传感器抽象体系复用） |

依赖：M3（传感器抽象体系）。

### M5 应急 mesh 自愈组网（灾害通讯地基）
| 交付物 | 说明 |
|---|---|
| 多跳动态路由 | AODV-lite 协议：RREQ/RREP/RERR，按需建路，帧级 TTL/hopCount |
| 拓扑发现 | 周期 HELLO 广播，邻居表维护（sysid→addr→lastSeen→linkQuality） |
| 自愈重构 | 邻居超时 → 标记链路断 → 触发 RERR → 受影响节点重新 RREQ |
| 路由度量 | 跳数 × W1 + RSSI × W2 + 端到端延迟 × W3 |
| 网状拓扑 | 不限链式，支持任意 DAG/网格拓扑 |
| MAVLink 新消息 | MeshHeartbeat(450) / MeshRouteRequest(451) / MeshRouteReply(452) / MeshRouteError(453) / MeshNeighborTable(454) |

依赖：M0a（一跳中继基础）。**M5 是 M6/M7/M8/M9 的核心地基。**

### M6 移动基站载荷抽象（无人机即基站）
| 交付物 | 说明 |
|---|---|
| 基站制式 | LTE micro-cell / WiFi mesh / LoRa 三种制式抽象，统一接口 |
| 覆盖模型 | 与地形、飞行高度、发射功率相关 |
| 容量模型 | 并发用户数 / 吞吐量 / 时延，超载触发拒绝或负载均衡 |
| 终端接入仿真 | 地面终端（手机/对讲机/传感器）注册、心跳、漫游切换 |
| 跨无人机漫游 | 终端从机 A 覆盖区移到机 B 覆盖区，无缝切换 |
| MAVLink 新消息 | CellTowerStatus(455) / CellTowerConfig(456) / CellHandover(457) / GroundTerminalRegister(458) |

依赖：M5（mesh 自愈组网提供机间通信 backbone）。

### M7 多层级中继：空-空-星-地（卫星中转）
| 交付物 | 说明 |
|---|---|
| LEO 星座模型 | 卫星轨道运动、可见窗口计算、过境切换、链路参数（延迟/带宽/中断） |
| 层级定义 | L0: 地面终端 → L1: 低空 mesh → L2: 高空中继机 → L3: LEO 卫星 → L4: 地面站 |
| 层级路由 | 近端优先：L1 可达不升 L2；不可达逐级升级 |
| 卫星链路模型 | 高延迟(50-500ms)、可中断（过境窗口）、带宽受限 |
| 跨层级切换 | 按策略：延迟最优 / 带宽最优 / 可靠性最优 |
| MAVLink 新消息 | SatLinkStatus(459) / SatPassSchedule(460) / HierarchicalRouteDecision(461) |

依赖：M5（mesh 路由基础）。

### M8 复杂地形适配（山地/森林/沼泽/老城/超高层）
| 交付物 | 说明 |
|---|---|
| 地形分类 | 山地/丘陵/森林/沼泽/老城密集/超高层/自然保护区 → TerrainType 枚举 |
| RF 传播增强 | 植被衰减(ITU-R P.833)、建筑穿透(ITU-R P.2109)、沼泽反射、多径、机间传播 |
| 飞行约束 | 限飞区（自然保护区）、安全高度（超高层附近）、风切变（山地）、沼泽禁飞 |
| 灾害地形变更 | 地震→建筑倒塌→遮挡模型动态更新；泥石流→地形高程变更 |
| MAVLink 新消息 | TerrainTypeMap(462) / TerrainUpdate(463) / FlightRestriction(464) |

依赖：M5（mesh 路由需地形感知做 NLOS 中继规划）。

### M9 应急任务编排（全流程闭环）
| 交付物 | 说明 |
|---|---|
| 全流程编排 | 灾区测绘 → 覆盖规划 → 组网部署 → 持续服务 → 自愈重构 |
| 覆盖优化 | 给定灾区范围 + 可用无人机数 → 最优部署方案 |
| 动态重构 | 无人机损毁 / 电量不足 → 重新规划拓扑 + 通知终端漫游 |
| 优先级调度 | 搜救通讯 > 指挥通讯 > 灾区测绘 > 常规巡检 |
| 灾害场景预设 | 地震/泥石流/火灾 预设场景，一键启动对应编排 |
| MAVLink 新消息 | EmergencyMissionPlan(465) / CoverageOptimization(466) / EmergencyPriority(467) |

依赖：M5 + M6 + M7 + M8（全部完成后才能编排）。

### M10 集群智能调度（多机任务分配 + 冲突避免） ✅ 已完成
| 交付物 | 说明 |
|---|---|
| 任务分配引擎 | 综合评分算法：能力匹配(40%) + 电量因子(30%) + 距离因子(20%) + 优先级(10%) |
| 冲突避免 | 航迹交叉检测 + 时空预留 + 安全距离约束 |
| 任务队列管理 | 优先级队列 + 全量重分配（无人机损毁后触发） |
| MAVLink 新消息 | TaskAssignmentMsg(468) / ConflictAlertMsg(469) / TaskStatusMsg(470) |

依赖：Baseline（DeviceRegistry 多机状态）。

### M11 自主决策引擎（AI 飞行策略） ✅ 已完成
| 交付物 | 说明 |
|---|---|
| 决策引擎 | 策略模式：返航(RTH) / 避障 / 自适应航迹，按场景切换 |
| 返航策略 | 电量/GPS/链路触发，最短安全路径 |
| 避障策略 | 雷达/视觉感知输入 → 速度限制 / 航点偏移 / 紧急悬停 |
| 自适应航迹 | 风/天气感知 → 航迹动态调整（复用 M0b 气象数据） |
| MAVLink 新消息 | DecisionEventMsg(471) / AdaptivePathMsg(472) |

依赖：M0b（气象数据）、M4（雷达感知）。

### M12 边缘计算节点（机载 AI 推理 + 传感器融合） ✅ 已完成
| 交付物 | 说明 |
|---|---|
| 边缘节点框架 | EdgeNode 抽象：任务接收 → 本地推理 → 结果上报 |
| 视频流分析 | 视频帧捕获 → 目标检测(模拟) → 结果回传 |
| 传感器融合 | IMU + GPS + 气压 + 视觉 → 融合状态估计（卡尔曼滤波简化） |
| 边缘协调 | cloud-backend 统一调度边缘任务分发 + 结果聚合 |
| MAVLink 新消息 | EdgeTaskStatusMsg(473) / SensorFusionDataMsg(474) |

依赖：M3（感知成像）、M11（决策引擎）。

### M13 数字孪生与预测（虚拟镜像 + 趋势预测） ✅ 已完成
| 交付物 | 说明 |
|---|---|
| 数字孪生服务 | 实时同步物理无人机状态 → 虚拟镜像（双向状态同步） |
| 轨迹预测 | 基于运动学模型 + 历史航迹 → 短期轨迹预测 |
| 场景回放 | 历史飞行数据 → 时间轴回放 + 对比分析 |
| 孪生对比 | 物理实体 vs 虚拟孪生 → 偏差检测 + 告警 |
| MAVLink 新消息 | TwinStateSyncMsg(475) / PredictionResultMsg(476) |

依赖：M12（边缘传感器融合数据源）。

### P2 灾害应急通讯组网扩展 ✅ 已完成
| 交付物 | 说明 |
|---|---|
| QoS 优先级队列 | 灾害场景通讯资源受限下的优先级保障（搜救>指挥>测绘>常规），带宽与转发资源按优先级分配 |
| 分簇路由 | 无人机群按地理/拓扑自动分簇，簇头负责簇内聚合与簇间转发，减少全局路由开销 |
| 异构链路桥接 | WiFi/LTE/LoRa/卫星等多链路碎片化覆盖下自动探测可用链路并按策略切换/聚合 |
| 灾区通信隔离 | 灾区内部通讯隔离保护，指定通道对外回传，防止外部干扰 |
| 灾害通信监控 | 实时监控灾区链路质量、节点存活、带宽利用率等指标 |
| 灾害态势面板 | GCS 端统一态势感知面板，可视化灾区通讯拓扑、链路状态、节点健康度 |
| 厂商协议适配层 | 海康/大华/宇视/ONVIF 统一适配，设备发现/视频拉取/PTZ 控制/事件订阅 |
| Emergency Budget 模式 | 两级预算：EMERGENCY_TOY（百元级，ESP-NOW+AMG8833+LED/蜂鸣器）/ EMERGENCY_STANDARD（千元级，LoRa Mesh+多链路+卫星中继） |
| 复杂地形飞行约束 | 地震倒塌遮挡动态更新、泥石流地形高程变更、火灾烟尘能见度影响 |
| 卫星中继增强 | 天通（高轨稳定覆盖）/铱星（低轨低延迟）/星链（低轨星座带宽最优），按灾区位置自动选择 |
| 空地协同指挥流程 | 六阶段指挥流程深化，空（无人机侦察/中继）地（安防设备/地面终端）协同 |
| 丐版 Mesh 路由 | ESP-NOW（近距离低延迟）/LoRa（远距离窄带），多跳转发+自愈重构，AODV-lite 轻量化替代 |
| AMG8833 热源搜救 | 8×8 红外热传感器阵列，废墟下热源检测与人员搜救 |
| LED/蜂鸣器控制 | 机载声光指引，帮助地面搜救人员定位无人机与标记目标 |
| LoRa 回传通道 | 窄带备用链路，主链路全部中断时保障最低限度指令传达 |
| GCS Emergency UI 适配 | Budget 模式切换/丐版 Mesh 拓扑可视化/热源搜救标记/声光控制面板 |
| MAVLink 新消息 | QoSRouteDecisionMsg(480) / ClusterFormationMsg(481) / DisasterModeStatusMsg(482) / BuzzerControlMsg(483) |

依赖：M5–M9（应急组网/基站/中继/地形/编排）、4a（空地一体化应急指挥）。

## 三、性能优化

| 编号 | 方向 | 交付物 |
|---|---|---|
| P1 | UDP 接收线程零分配 | 帧解码对象池化 + GC 抖动消除 |
| P2 | WebSocket 批量推送 | 聚合 50ms 窗口帧 + 单次广播，减少 syscall |

## 四、工程完善（6 方向）

| 方向 | 交付物 | 状态 |
|---|---|---|
| ① 安全认证 | JWT 令牌 + Spring Security + dev-mode 白名单 + 多租户隔离 + 审计日志 + License 管理 | ✅ 已完成 |
| ② 可观测性 | Micrometer 指标 + 健康检查 + 结构化日志(logback-spring.xml) | ✅ 已完成 |
| ③ API 文档 | OpenAPI 3.0 + Swagger UI + 全局 Bearer 认证配置 | ✅ 已完成 |
| ④ 集成测试 | CI 集成测试脚本 + GitHub Actions integration job | ✅ 已完成 |
| ⑤ 前端增强 | 实时遥测图表 + 仪表盘面板 + 移动端响应式 + 39 个功能面板 | ✅ 已完成 |
| ⑥ 部署运维 | Dockerfile(3) + docker-compose + K8s 清单(7) + Helm chart(7) + nginx | ✅ 已完成 |

## 五、依赖图

```
M0a(mesh一跳) ──┐
                ├──► M1(编队表演) ──► 演示闭环
M0b(气象) ──────┼──► M2(喷洒/物流)
                └──► (横切，供 M1/M2/M11 复用)

Baseline(感知) ──► M3(成像增强) ──► M4(硬件抽象)

M0a ──► M5(mesh自愈) ──┬──► M6(移动基站) ──┐
                        ├──► M7(星-空-地) ──┤
                        └──► M8(复杂地形) ──┤
                                             └──► M9(应急编排)

Baseline ──► M10(集群调度)
M0b + M4 ──► M11(自主决策)
M3 + M11 ──► M12(边缘计算) ──► M13(数字孪生)

M5+M6+M7+M8+M9 ──► 4a(空地一体化应急指挥) ──► P2(灾害应急通讯组网扩展)
```

- M0a / M0b / M3 彼此独立，可并行；M1 依赖 M0a；M2 依赖 M0a+M0b；M4 依赖 M3。
- M5 依赖 M0a；M6/M7/M8 依赖 M5 且可并行；M9 依赖 M5+M6+M7+M8。
- M10 依赖 Baseline；M11 依赖 M0b+M4；M12 依赖 M3+M11；M13 依赖 M12。
- P2 依赖 M5–M9 + 4a，是灾害应急通讯组网的全面扩展。
- 相控阵雷达、真实气动、真实 CV、移动基站、卫星链路均为「软件层协议抽象 + 模拟器假数据源」，未来接真硬件替换数据源，架构不变。

## 六、执行纪律

1. 每个里程碑走完整 SDD；单个里程碑内尽量原子化（2–4h/任务）。
2. 代码严格落在已有模块边界内：`cloud-backend`(调度/API)、`drone-sim`(载荷/执行)、
   `mavlink-core`(新消息)、`link-sim`(中继/链路)、`gcs-web`(观察)。
3. 每个里程碑必须有回归基线：现有 3230 单测 + e2e 脚本不回归。
4. 边界诚实声明：工作量 = 协议抽象 + 假数据源，非真硬件实现。
5. **MAVLink msgId 全局唯一**：已分配 420-441(M0a-M4)、450-467(M5-M9)、468-476(M10-M13)、477-479(4a 安防报警)、480-483(P2 灾害应急通讯组网扩展)，新增须避开这些区间。